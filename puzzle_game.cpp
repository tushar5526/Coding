//
#include<bits/stdc++.h>

#define mod 1000000007
#define lli long long int
#define endl '\n'
using namespace std;
#define REP(i,n) for (int i = 0; i < n; i++)

 
int main()
{
	int t;
	cin>>t;
	while(t--)
	{
        vector<int> a;
        REP(i,9)
        {
            int tmp;
            cin>>tmp;
            a.push_back(tmp);
        }

        int flag = 0;
        
        while(flag == 0)
        {
            REP(i,9)
            {
                
            }
        }
    }
    return 0;
}